// node {
// 	stage('Build') {
// 		echo "Build"
// 	}
// 	stage('Test') {
// 		echo "Test"
// 	}

// 	stage('Ingration test'){
// 		echo "Intergration Test"
// 	}

// }

pipeline {
	agent any

	stages {
		stage('Build') {
			steps {
				echo "Build"
			}
		}

		stage('Build') {
			steps {
				echo "Build"
			}
		}

		stage('Build') {
			steps {
				echo "Build"
			}
		}
		
	}
	
}